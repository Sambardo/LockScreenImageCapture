<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Fetch Lockscreen Image" Height="450" Width="800">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="296*"/>
            <RowDefinition Height="65*"/>
            <RowDefinition Height="59*"/>
        </Grid.RowDefinitions> 
        <Button x:Name="FetchButton" Content="Save Image" Margin="0" Grid.Row="2" MinWidth="200" MinHeight="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Image x:Name="ImageBox" Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center" MinHeight="0" MinWidth="0"/>
        <TextBox x:Name="InputBox" Grid.Row="1" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" MinHeight="40" MinWidth="195"/>

    </Grid>
</Window>
